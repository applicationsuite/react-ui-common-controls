export const rangeFilterStyles = (theme) => ({
    toText: {
        fontWeight: 400,
        padding: '5px'
    },
    accordionHeader: {
        '& h2': {
            fontSize: 14,
            cursor: 'pointer'
        }
    }
});
