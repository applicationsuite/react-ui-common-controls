export const rangeFilterStyles = (theme: string) => ({
  toText: {
    fontWeight: 400,
    padding: '5px'
  },
  accordionHeader: {
    '& h2': {
      fontSize: 14,
      cursor: 'pointer'
    }
  }
});
