export const toggleFilterStyles = (theme) => ({
    accordionHeader: {
        '& h2': {
            fontSize: 14,
            cursor: 'pointer'
        }
    },
    accordianBody: {
        paddingLeft: '6px'
    }
});
